var expect = require('chai').expect;
var functions = require('../functions.js');

describe('isDiagonalMatrix', function() {
	it('should exist', function() {
		expect(functions.isDiagonalMatrix).not.to.be.undefined;
	});
});